#!/usr/bin/env bash
set -e -x

LOG_DIR=/var/vcap/sys/log/iem_agent
JOB_NAME=iem_agent

mkdir -p ${BOSH_INSTALL_TARGET}
cp -a ${JOB_NAME}/* ${BOSH_INSTALL_TARGET}
echo "bosh install target is: ${BOSH_INSTALL_TARGET}, copy iem_install package"
ls -l ${BOSH_INSTALL_TARGET}

